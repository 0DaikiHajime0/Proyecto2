# Configuracion de MLflow remoto en DagsHub (opcional).
# Si defines estas variables, los experimentos se envian automaticamente
# al repositorio remoto de DagsHub (ver src/models/train_model.py).
# Deja los valores vacios para usar el tracking local (sqlite: mlflow.db).

DAGSHUB_USER=
DAGSHUB_TOKEN=
DAGSHUB_REPO=mle-project2-spotify-popularity
